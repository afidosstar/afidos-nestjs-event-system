# Configuration de l'exemple basic-usage

# Base de données
NODE_ENV=development

# Email (SMTP) - Configuration requise pour les notifications email
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=noreply@yourapp.com

# Telegram (optionnel) - Pour les notifications Telegram
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# Webhooks (optionnel) - Pour les notifications webhook
WEBHOOK_URL=https://your-webhook-url.com/notifications

# Redis (optionnel) - Pour la gestion des queues
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Mode de fonctionnement
# - api: Traitement immédiat uniquement
# - worker: Queue Redis obligatoire, traitement différé uniquement  
# - hybrid: Queue si disponible, sinon traitement immédiat
MODE=hybrid

# Telegram pour tests (optionnel)
# Remplacez par votre vrai chat ID pour recevoir les notifications
USER_TELEGRAM_ID=123456789