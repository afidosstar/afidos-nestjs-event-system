<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Commande expédiée #{{orderId}}</title>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: #17a2b8; color: white; text-align: center; padding: 20px; border-radius: 8px 8px 0 0; }
        .content { background: #f9f9f9; padding: 30px; border-radius: 0 0 8px 8px; }
        .tracking-info { background: white; padding: 20px; border-radius: 6px; margin: 20px 0; border-left: 4px solid #17a2b8; }
        .button { display: inline-block; background: #17a2b8; color: white; padding: 12px 24px; text-decoration: none; border-radius: 4px; margin: 10px 0; }
        .footer { text-align: center; font-size: 12px; color: #666; margin-top: 20px; }
        .tracking-number { font-family: 'Courier New', monospace; background: #f8f9fa; padding: 8px; border-radius: 4px; font-weight: bold; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🚚 Commande expédiée !</h1>
        </div>
        
        <div class="content">
            <p>Bonjour {{userName}},</p>
            
            <p>Bonne nouvelle ! Votre commande <strong>#{{orderId}}</strong> a été expédiée et est en route vers vous.</p>
            
            {{#if trackingNumber}}
            <div class="tracking-info">
                <h3>Informations de suivi</h3>
                <p><strong>Numéro de suivi :</strong></p>
                <p class="tracking-number">{{trackingNumber}}</p>
                <p><strong>Transporteur :</strong> {{carrier}}</p>
                <p><strong>Date d'expédition :</strong> {{currentDate}}</p>
                {{#if estimatedDelivery}}
                <p><strong>Livraison estimée :</strong> {{estimatedDelivery}}</p>
                {{/if}}
            </div>
            {{/if}}
            
            <p>Vous pouvez suivre l'acheminement de votre colis en temps réel grâce au numéro de suivi fourni ci-dessus.</p>
            
            <div style="text-align: center; margin: 30px 0;">
                {{#if trackingNumber}}
                <a href="{{trackingLink trackingNumber}}" class="button">Suivre mon colis</a>
                <br><br>
                {{/if}}
                <a href="{{appUrl}}/orders/{{orderId}}" class="button" style="background: #6c757d;">Voir ma commande</a>
            </div>
            
            <p>Si vous avez des questions ou rencontrez un problème lors de la livraison, n'hésitez pas à <a href="{{supportUrl}}">contacter notre service client</a>.</p>
            
            <p>Merci pour votre confiance !</p>
            
            <p>L'équipe {{companyName}}</p>
        </div>
        
        <div class="footer">
            <p>© {{currentYear}} {{companyName}}. Tous droits réservés.</p>
            <p>
                <a href="{{appUrl}}">Site web</a> | 
                <a href="{{supportUrl}}">Support</a> | 
                <a href="{{unsubscribeUrl}}">Se désabonner</a>
            </p>
        </div>
    </div>
</body>
</html>